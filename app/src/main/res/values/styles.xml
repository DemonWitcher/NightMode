<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <!--整体背景色 -->
    <!--区域背景色 -->
    <!--字体颜色-->
    <!--分割线颜色-->
    <style name="DayTheme" parent="AppTheme">
        <item name="all_bg_color">@color/all_bg_color_day</item>
        <item name="view_bg_color">@color/view_bg_color_day</item>
        <item name="tv_color">@color/tv_color_day</item>
        <item name="divider_color">@color/divider_color_day</item>
    </style>

    <style name="NightTheme" parent="AppTheme">
        <item name="all_bg_color">@color/all_bg_color_night</item>
        <item name="view_bg_color">@color/view_bg_color_night</item>
        <item name="tv_color">@color/tv_color_night</item>
        <item name="divider_color">@color/divider_color_night</item>
    </style>
</resources>
